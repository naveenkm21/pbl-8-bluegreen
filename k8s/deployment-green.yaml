apiVersion: apps/v1
kind: Deployment
metadata:
  name: pbl8-app-green
  labels:
    app: pbl8-app
    color: green
spec:
  replicas: 3
  selector:
    matchLabels:
      app: pbl8-app
      color: green
  template:
    metadata:
      labels:
        app: pbl8-app
        color: green
    spec:
      containers:
        - name: pbl8-app
          image: REPLACE_ME_IMAGE
          ports:
            - containerPort: 5000
          env:
            - name: APP_COLOR
              value: "green"
            - name: APP_VERSION
              value: "v2"
          readinessProbe:
            httpGet: { path: /health, port: 5000 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 5000 }
            initialDelaySeconds: 15
            periodSeconds: 20
