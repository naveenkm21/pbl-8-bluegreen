# Preview service — always points to the INACTIVE color for pre-cutover testing.
apiVersion: v1
kind: Service
metadata:
  name: pbl8-app-preview
spec:
  type: NodePort
  selector:
    app: pbl8-app
    color: green
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30081
